apiVersion: 1

datasources:
    - name: Loki
      type: loki
      access: proxy
      url: http://loki:3100
      isDefault: true
      editable: false
      jsonData:
          maxLines: 1000

# === 1-B: Prometheus 도입 시 아래 주석 해제 ===
#     - name: Prometheus
#       type: prometheus
#       access: proxy
#       url: http://prometheus:9090
#       editable: false
