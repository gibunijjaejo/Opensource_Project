# 앱 서버용 관측 스택 — Promtail만 (로그를 원격 Loki로 push)
# 사용처: 앱 서버 (163.239.77.77)
# 사용법: docker compose -f docker-compose.yml -f docker-compose.observability.app.yml up -d
# 또는: make prod-up-obs-app

services:
    promtail:
<<<<<<< HEAD
        image: grafana/promtail:3.3.2
        container_name: seoganpyo-promtail
        restart: unless-stopped
        volumes:
            - ./infra/observability/promtail/promtail-config.prod.yml:/etc/promtail/config.yml:ro
            - /var/run/docker.sock:/var/run/docker.sock:ro
            - /var/lib/docker/containers:/var/lib/docker/containers:ro
            - promtail_positions:/tmp
        command: -config.file=/etc/promtail/config.yml

volumes:
    promtail_positions:
